//! Port of `kquery/examples/src/main/kotlin/NYCTaxi.kt`.
//! Canonical smoke test once the `execution` crate is in place.

fn main() {
    env_logger::init();
    todo!("port kquery/examples/.../NYCTaxi.kt")
}
