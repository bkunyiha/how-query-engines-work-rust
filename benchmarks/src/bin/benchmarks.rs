//! Port of `kquery/benchmarks/src/main/kotlin/Benchmarks.kt`.
//! Aggregate query against NYC taxi CSV files, in parallel.

fn main() {
    env_logger::init();
    todo!("port kquery/benchmarks/.../Benchmarks.kt")
}
